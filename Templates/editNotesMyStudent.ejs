<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('partials/links') %> <%- include('partials/style') %>

    <title>Edit Notes</title>
  </head>
  <body class="fst-italic">
    <%- include('partials/navBarTeacher', {title:"Edit Notes"}) %>
    <main class="p-0 mt-4 container d-flex justify-content-center w-75">
      <form
        action="/teacher/class/notes/save/?student=<%=student._id  %>&teacher=<%=teacher._id  %>"
        method="post"
        class="p-3 shadow rounded text-light"
        style="background-color: #9cc4e4; transform: translateY(20vh)"
      >
        <h4>Discipline: <%=teacher.schoolDiscipline %></h4>

        <div class="d-flex">
          <h4 class="mb-0 align-self-center me-2">Note 1:</h4>
          <input
            type="text"
            name="notes.<%=teacher.schoolDiscipline  %>.note1"
            value="<%= notes.note1  %>"
            class="w-25 form-control d-flex text-center"
            onchange="this.value = this.value.replace(/,/g, '.')"
            required
          />
        </div>

        <div class="d-flex">
          <h4 class="mb-0 align-self-center">Note 2:</h4>
          <input
            type="text"
            name="notes.<%=teacher.schoolDiscipline  %>.note2"
            value="<%= notes.note2  %>"
            class="w-25 ms-1 form-control d-flex text-center"
            onchange="this.value = this.value.replace(/,/g, '.')"
            required
          />
        </div>

        <div class="d-flex">
          <h4 class="mb-0 align-self-center">Note 3:</h4>
          <input
            type="text"
            name="notes.<%=teacher.schoolDiscipline  %>.note3"
            value="<%= notes.note3  %>"
            class="w-25 ms-1 form-control d-flex text-center"
            onchange="this.value = this.value.replace(/,/g, '.')"
            required
          />
        </div>
        <div class="justify-content-center d-flex mt-2">
          <button
            type="submit"
            class="btn btn-light me-2"
            style="color: #9cc4e4"
          >
            Save
          </button>

          <a
            class="btn btn-danger d-flex"
            href="/teacher/class/notes/?student=<%=student._id  %>&token=<%=token  %>"
          >
            Return
          </a>
        </div>
      </form>
    </main>

    <%- include('partials/footer') %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
