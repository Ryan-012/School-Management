<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('partials/links') %> <%- include('partials/style') %>
    <title>Create Message</title>
  </head>
  <body>
    <%- include('partials/navBarTeacher', {title:"Create Message"}) %>
    <main
      class="container p-3 shadow rounded w-50"
      style="background-color: #9cc4e4; transform: translateY(15vh)"
    >
      <form
        class="form text-light"
        action="/teacher/class/message/save/?student=<%=student._id  %>&teacher=<%=teacher._id  %>"
        method="post"
        autocomplete="off"
      >
        <div>
          <h3>Create By:</h3>
          <input
            type="text"
            name="createBy"
            onchange="this.value = this.value.trim()"
            class="form-control"
            value="<%=teacher.completeName%> | <%=teacher.schoolDiscipline%>"
            required
          />
        </div>
        <div>
          <h3>Message:</h3>
          <input
            type="text"
            name="message"
            onchange="this.value = this.value.trim()"
            class="form-control"
            required
          />
        </div>
        <div>
          <h3>Message To:</h3>
          <input
            type="text"
            name="messageTo"
            onchange="this.value = this.value.trim()"
            class="form-control"
            value="<%=student.completeName  %> | <%=student.class  %>"
            required
          />
        </div>
        <div class="justify-content-center d-flex mt-2">
          <button
            type="submit"
            class="me-2 btn btn-light d-flex"
            style="color: #9cc4e4"
          >
            Create
          </button>
          <a
            class="btn btn-danger d-flex"
            href="/teacher/class/?token=<%=token  %>&class=<%=student.class  %>"
          >
            Return
          </a>
        </div>
      </form>
    </main>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
